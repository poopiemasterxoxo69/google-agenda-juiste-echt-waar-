// manual_plus.js (wrapper phase)
(function(){
  console.log('[BOOT] manual_plus.js loaded');
  // This module currently binds no named functions (logic is DOMContentLoaded based in script_tool.js)
  // We still expose a namespace for future extraction if needed.
  window.ManualPlus = window.ManualPlus || {};
  console.log('[OK] ManualPlus namespace bound');
})();
